diff --git a/transfuser/train.py b/transfuser/train.py
index 681df8a..22c7a3c 100644
--- a/transfuser/train.py
+++ b/transfuser/train.py
@@ -28,6 +28,7 @@ parser.add_argument('--batch_size', type=int, default=24, help='Batch size')
 parser.add_argument('--logdir', type=str, default='log', help='Directory to log data to.')
 parser.add_argument('--pc_bb', type=str, default='bev', help='bev or pointpillars for pointcloud processing backbone')
 parser.add_argument('--wandb', dest='wandb', action='store_true')
+parser.add_argument('--seed', type=int, default=10, help='seed')
 
 args = parser.parse_args()
 args.logdir = os.path.join(args.logdir, args.id)
@@ -38,7 +39,7 @@ if args.wandb:
 		project='Transfuser',
 	)
 
-def seed_all(seed: int):
+def seed_all(seed: int = 10):
 	import random
 	import numpy as np
 	import torch
@@ -52,7 +53,7 @@ def seed_all(seed: int):
 		torch.backends.cudnn.deterministic = True
 		torch.backends.cudnn.benchmark = False
 
-seed_all()
+seed_all(args.seed)
 
 class Engine(object):
 	"""Engine that runs training and inference.
@@ -137,6 +138,8 @@ class Engine(object):
 			optimizer.step()
 
 			writer.add_scalar('train_loss', loss.item(), self.cur_iter)
+			from IPython import embed
+			embed()
 			if args.wandb:
 				wandb.log({'train_loss', loss.item()})
 			self.cur_iter += 1
